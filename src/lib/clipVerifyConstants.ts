/** Shared numbers only — safe to import from client pages without pulling in Transformers.js. */
export const DEFAULT_CLIP_MODEL_ID = "Xenova/clip-vit-base-patch32";
export const DEFAULT_CLIP_VERIFY_THRESHOLD = 0.65;
